# CreativeIQ backend — local FastAPI server + (optional) Bedrock
fastapi==0.111.0
uvicorn[standard]==0.30.1
# boto3 is only needed when USE_BEDROCK=1 (real Bedrock). Offline demo needs neither.
boto3>=1.34.0
# mangum wraps the FastAPI app for AWS Lambda (deploy only).
mangum==0.19.0
